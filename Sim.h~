#include<vector>
#include<SDL.h>

#include"Particle.h"

class Sim
{
	public:
		
	private: 
		std::vector<Particle> particles;
		float deltaTime;
		SDL_Surface* surface = NULL;		
		int simWidth, simHeight;
	public: 
		Sim(int simWidth, int simHeight, int numParticles);
		void run();
	private:
		void update(float deltaTime);
		void render();
	
};	
